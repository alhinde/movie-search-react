{
  "name": "movie-search-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "core-js": "^2.4.1",
    "lodash": "^4.17.4",
    "material-ui": "^0.17.0",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-redux": "^5.0.3",
    "react-router": "^3.0.2",
    "superagent": "^3.5.0"
  },
  "devDependencies": {
    "react-scripts": "0.9.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "react-scripts build && git add build && git commit -m 'deploying to github pages' && git subtree push --prefix build origin gh-pages",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
